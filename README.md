# chimera.github.io
Project page for chimera.

Static HTML site (no build step) served via GitHub Pages at https://chimeraproject.io/.
Edit the `.html` files and `styles.css` directly; pushes to `main` are deployed by
`.github/workflows/pages.yml`.
